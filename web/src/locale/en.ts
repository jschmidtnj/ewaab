import Messages from './type';

const englishMessages: Messages = {
  'sign up': 'sign up',
  login: 'login',
  register: 'register',
  reset: 'reset',
  profile: 'profile',
  'sign out': 'sign out',
  users: 'users',
  account: 'account',
};

export default englishMessages;
